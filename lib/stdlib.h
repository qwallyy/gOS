/* gOS Kernel — Standard Library */

#ifndef GOS_STDLIB_H
#define GOS_STDLIB_H

#include "gOS/types.h"

int atoi(const char *s);
long atol(const char *s);
int64_t atoll(const char *s);

#endif /* GOS_STDLIB_H */
